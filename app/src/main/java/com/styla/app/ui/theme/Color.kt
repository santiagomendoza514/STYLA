package com.styla.app.ui.theme

import androidx.compose.ui.graphics.Color

// Base — fondos y texto principal
val OffWhite = Color(0xFFFAF8F5)      // Fondo principal warm off-white
val Charcoal = Color(0xFF1A1A1A)      // Texto principal, dark mode bg
val SandBeige = Color(0xFFE8DFD3)     // Cards, surfaces, divisores cálidos

// Acento — CTAs y highlights
val Coral = Color(0xFFFF4B4B)         // Color signature de STYLA
val CoralDark = Color(0xFFD83A3A)     // Pressed states
val CoralLight = Color(0xFFFFE4E4)    // Backgrounds suaves de acento

// Grises — texto secundario y bordes
val SoftGray = Color(0xFF8A8A8A)      // Texto secundario
val LightGray = Color(0xFFE5E5E5)     // Bordes, separadores
val MidGray = Color(0xFF4A4A4A)       // Texto sobre fondos oscuros

// Semánticos
val SuccessGreen = Color(0xFF2E7D5B)
val ErrorRed = Color(0xFFD32F2F)

// Categorías — colores únicos por tipo de tienda (para pins/tags)
val CategoryStreetwear = Color(0xFF1A1A1A)
val CategoryLuxury = Color(0xFF8B6F47)
val CategoryVintage = Color(0xFF7A5C3E)
val CategoryDesigner = Color(0xFFFF4B4B)
val CategoryAccessories = Color(0xFFC8A882)